name: Restore Mosigo Vercel Drop

on:
  push:
    branches:
      - recovery/bootstrap

permissions:
  contents: write

jobs:
  restore:
    runs-on: ubuntu-latest
    steps:
      - name: Check out bootstrap branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Restore exact source from Vercel Drop
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          DEPLOYMENT_ID: dpl_HiwGaYsHZfrx9HdET6nHk9FKLjAj
          TEAM_ID: team_UKIfvGHoYZXtomQSqEztbBDr
          EXPECTED_BYTES: "26703616"
          EXPECTED_FINGERPRINT: 93b51d43ebd7d8d725bdefe03b1770c158842270d6608e613fde7dc76c6d77b9
        run: |
          set -euo pipefail
          test -n "${VERCEL_TOKEN:-}"
          python3 - <<'PY'
          import base64
          import hashlib
          import json
          import os
          import pathlib
          import shutil
          import urllib.parse
          import urllib.request
          from concurrent.futures import ThreadPoolExecutor, as_completed

          expected_paths = {
              "src/0810(4).mp4", "src/12.png", "src/13.png", "src/14.png", "src/Group 7385.png",
              "src/api/hospitals.js", "src/bar.png", "src/bg.webp", "src/bottom.png", "src/car.png",
              "src/cat-icons/baby.webp", "src/cat-icons/bone.webp", "src/cat-icons/ear.webp", "src/cat-icons/eye.webp",
              "src/cat-icons/heart.webp", "src/cat-icons/lotion.webp", "src/cat-icons/mind.webp", "src/cat-icons/pill.webp",
              "src/clinic-logo.png", "src/day.png", "src/finish.png", "src/game1.webp", "src/game2.mp4", "src/game3.mp4",
              "src/game4.mp4", "src/game_img1.webp", "src/gyeote_report_hero.png", "src/index.html", "src/location.png",
              "src/mgr/1.webp", "src/mgr/10.webp", "src/mgr/11.webp", "src/mgr/2.webp", "src/mgr/3.webp",
              "src/mgr/4.webp", "src/mgr/5.webp", "src/mgr/6.webp", "src/mgr/7.webp", "src/mgr/8.webp", "src/mgr/9.webp",
              "src/new_event.html", "src/new_ext-pages.js", "src/new_game.html", "src/new_montage.css", "src/new_roles.css",
              "src/vercel.json"
          }

          token = os.environ["VERCEL_TOKEN"]
          deployment = os.environ["DEPLOYMENT_ID"]
          team = os.environ["TEAM_ID"]
          expected_bytes = int(os.environ["EXPECTED_BYTES"])
          expected_fingerprint = os.environ["EXPECTED_FINGERPRINT"]
          api = "https://api.vercel.com"
          out = pathlib.Path(os.environ["RUNNER_TEMP"]) / "mosigo-source"
          shutil.rmtree(out, ignore_errors=True)
          out.mkdir(parents=True, exist_ok=True)

          def api_get(url):
              req = urllib.request.Request(
                  url,
                  headers={
                      "Authorization": f"Bearer {token}",
                      "Accept": "application/json, */*",
                      "User-Agent": "mosigo-github-restore/1.1",
                  },
              )
              with urllib.request.urlopen(req, timeout=90) as resp:
                  return resp.read()

          def safe_rel(parts):
              clean = []
              for part in parts:
                  if not part or part in (".", "..") or "/" in part or "\\" in part:
                      raise RuntimeError(f"unsafe path component: {part!r}")
                  clean.append(part)
              return pathlib.PurePosixPath(*clean).as_posix()

          def flatten(nodes, parent=()):
              files = []
              for node in nodes:
                  if not isinstance(node, dict) or not isinstance(node.get("name"), str):
                      continue
                  cur = parent + (node["name"],)
                  children = node.get("children") or []
                  typ = str(node.get("type") or "").lower()
                  uid = node.get("uid")
                  if children or typ in ("directory", "dir", "folder"):
                      files.extend(flatten(children, cur))
                  elif uid:
                      files.append({"path": safe_rel(cur), "uid": str(uid)})
              return files

          q = urllib.parse.urlencode({"teamId": team})
          tree = json.loads(api_get(f"{api}/v6/deployments/{deployment}/files?{q}").decode("utf-8"))
          all_files = flatten(tree)
          by_path = {item["path"]: item for item in all_files}
          missing = sorted(expected_paths - set(by_path))
          if missing:
              raise RuntimeError(f"expected Vercel source paths missing: {missing}")
          selected = [by_path[path] for path in sorted(expected_paths)]
          extra = sorted(set(by_path) - expected_paths)
          print(f"Vercel returned {len(all_files)} files; restoring the approved {len(selected)} paths only.")
          if extra:
              print("Ignored extra paths: " + ", ".join(extra))

          def fetch(item):
              uid = urllib.parse.quote(item["uid"], safe="")
              raw = api_get(f"{api}/v8/deployments/{deployment}/files/{uid}?{q}")
              text = raw.decode("utf-8")
              try:
                  parsed = json.loads(text)
                  if isinstance(parsed, str):
                      payload = parsed
                  elif isinstance(parsed, dict):
                      payload = next((parsed[k] for k in ("data", "content", "body", "file", "value") if isinstance(parsed.get(k), str)), None)
                  else:
                      payload = None
              except json.JSONDecodeError:
                  payload = text.strip()
              if not payload:
                  raise RuntimeError(f"missing payload for {item['path']}")
              payload += "=" * ((4 - len(payload) % 4) % 4)
              data = base64.b64decode(payload)
              target = out / pathlib.PurePosixPath(item["path"])
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_bytes(data)
              return item["path"], len(data)

          total = 0
          with ThreadPoolExecutor(max_workers=6) as pool:
              futures = {pool.submit(fetch, item): item for item in selected}
              for fut in as_completed(futures):
                  path, size = fut.result()
                  total += size
                  print(f"restored {path} ({size} bytes)")

          paths = sorted(p for p in out.rglob("*") if p.is_file())
          if len(paths) != 46:
              raise RuntimeError(f"restored file count mismatch: {len(paths)} != 46")
          if total != expected_bytes:
              raise RuntimeError(f"byte count mismatch: {total} != {expected_bytes}")

          h = hashlib.sha256()
          for path in paths:
              rel = path.relative_to(out).as_posix().encode()
              data = path.read_bytes()
              h.update(len(rel).to_bytes(4, "big"))
              h.update(rel)
              h.update(len(data).to_bytes(8, "big"))
              h.update(data)
          fingerprint = h.hexdigest()
          if fingerprint != expected_fingerprint:
              raise RuntimeError(f"fingerprint mismatch: {fingerprint} != {expected_fingerprint}")

          print(f"VERIFIED {len(paths)} files / {total} bytes / {fingerprint}")
          PY

      - name: Commit verified source to recovery branch
        run: |
          set -euo pipefail
          git config user.name "mosigo-restore-bot"
          git config user.email "actions@users.noreply.github.com"
          git checkout -B recovery/vercel-drop
          find . -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} +
          cp -a "$RUNNER_TEMP/mosigo-source/." .
          git add -A
          git commit -m "Restore exact Mosigo source from Vercel Drop"
          git push --force origin HEAD:refs/heads/recovery/vercel-drop
          echo "RECOVERY_COMMIT=$(git rev-parse HEAD)"
