{
  "name": "mosigo-repository-quality",
  "version": "2.0.0",
  "private": true,
  "description": "Quality checks for the Mosigo v2 prototype repository.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=24"
  }
}
