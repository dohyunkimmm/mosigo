{
  "name": "mosigo-repository-quality",
  "version": "7.0.0",
  "private": true,
  "description": "Quality and source-structure checks for the Mosigo prototype repository.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "audit:source": "node scripts/source-audit.js",
    "quality": "npm test && npm run audit:source",
    "smoke:production": "node scripts/production-smoke.js"
  },
  "engines": {
    "node": ">=24"
  }
}
